(import lit_10 () (ext Int))
(import lit_-1 () (ext Int))
(import add (ext Int ext Int) (ext Int))
(import ifz (ext Int) () ())
(import return (ext Int))

(define main () ()
  (extern lit_10 () ((i)
  (jump loop)))
)

(define loop (i) (ext Int)
  (extern ifz (i)
    (()
      (extern return (i)))
    (()
      (extern lit_-1 () ((x)
      (extern add (i x) ((y)
      (substitute (y) ((i)
      (jump loop)))))))))
)
