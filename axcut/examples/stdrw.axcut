(import lit_5 () (ext Int))
(import mmap_anon_page () (ext Page))
(import munmap_page (ext Page) ())
(import read_stdin (ext Page ext Int) (ext Int))
(import write_stdout (ext Page ext Int) (ext Int))
(import return (ext Int))

(define main () ()
  (do (c) extern lit_5 ())
  (do (m) extern mmap_anon_page ())
  (do (r) extern read_stdin (m c))
  (do (w) extern write_stdout (m r))
  (do () extern munmap_page (m))
  (extern return (w))
)
