(import lit_1 () (ext Int))
(import lit_9 () (ext Int))
(import add (ext Int ext Int) (ext Int))
(import return (ext Int))

(signature Cont (ext Int))
(signature Func (con Cont ext Int))

(define main () ()
  (do (a) extern lit_9 ())
  (new f Func (a) ((k x)
    (do (b) extern add (a x))
    (do (k b) substitute (k b))
    (invoke k 0)))
  (new k Cont () ((r) (extern return (r))))
  (do (y) extern lit_1 ())
  (do (f k y) substitute (f k y))
  (invoke f 0)
)
